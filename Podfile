# Uncomment the next line to define a global platform for your project
platform :osx, '10.15'

target 'mac-entitlements' do
  use_frameworks!
  use_modular_headers!
  pod 'FirebaseCore'
  pod 'FirebaseInstallations'
end
